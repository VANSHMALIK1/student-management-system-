# student-management-system-
The Student Management System is a Java-based desktop application designed to records efficiently. The system allows administrators to perform CRUD operations (Create, Read, Update, Delete) on student data stored in a MySQL relational database. 
